# Copy this to ".env" if your backend is not on the default address.
# Vite only sends variables starting with VITE_ to the browser, so never put
# a secret in here — anyone can read it in the page source.

VITE_API_URL=http://localhost:5000/api
